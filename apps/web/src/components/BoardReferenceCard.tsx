export function BoardReferenceCard() {
  return (
    <figure className="card board-reference-card">
      <span className="label">BOARD SOURCE STATUS</span>
      <p className="board-reference-status">The physical board is still being transcribed. Play uses the development fixture for now.</p>
      <figcaption>
        Reference photographs are for review only.
      </figcaption>
    </figure>
  );
}
